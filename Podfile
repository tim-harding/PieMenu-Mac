# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'PieMenu' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for PieMenu
  pod 'KeyHolder', :git => 'https://github.com/Clipy/KeyHolder.git', :branch => 'swift4'

  target 'PieMenuTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'PieMenuUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
